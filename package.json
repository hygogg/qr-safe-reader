{
  "name": "qr-safe-reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server/index.mjs",
    "start": "node server/index.mjs",
    "test": "node --test"
  }
}
